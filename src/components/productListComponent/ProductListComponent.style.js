import {StyleSheet} from 'react-native';
import {WP} from '../../utilities';

export const styles = StyleSheet.create({
  separatorView: {height: WP(2), width: WP(40)},
  container: {
    paddingHorizontal: WP(4),
    paddingTop: WP(4),
  },
});
